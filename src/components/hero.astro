---
import ChevronRightIcon from "@icons/chevron-right.svg"
import Button from "./button.astro"
import { Picture } from "astro:assets"
import mansoryImage from "@images/mansory.png"
---

<div id="about">
  <div class="container">
    <div>
      <h1>Instalação e Manutenção de Ar Condicionado e Geladeiras</h1>

      <p>
        Temos os melhores profissionais especialistas em climatização e
        refrigeração.
      </p>

      <Button>
        Entrar em contato
        <ChevronRightIcon aria-hidden />
      </Button>
    </div>

    <Picture
      src={mansoryImage}
      loading="eager"
      alt="Profissionais reparando sistemas de climatização e de refrigeração"
    />
  </div>
</div>

<style>
  #about {
    background: linear-gradient(white, hsla(0, 0%, 100%, 0.1)),
      url("../assets/images/background-pattern.png"), var(--neutral-100);
    border-bottom: solid 1px var(--gray-200);
  }

  .container {
    display: flex;
    flex-direction: column;
    gap: 40px;
    padding: 64px 16px;
  }

  h1 {
    line-height: 1.2;
    margin-bottom: 16px;
  }

  p {
    margin-bottom: 40px;
  }

  button {
    border-radius: 24px;
  }

  picture {
    margin-inline: auto;
  }

  @media (min-width: 1024px) {
    .container {
      flex-direction: row;
      justify-content: center;
      align-items: center;

      gap: 56px;
      padding-block: 80px;
    }

    .container > div {
      max-width: 401px;
    }

    picture {
      margin-inline: 0;
    }

    p {
      font-size: 1.5rem;
      font-weight: 400;
      line-height: 1;
    }
  }
</style>
